const initialState = {
    message: `It's easy to integrate React and Redux, isn't it?, WRC is the best`,
  };
  
  function reducer(state = initialState) {
    return state;
  }
  
  export default reducer;